name: Staging synthetic receiver setup

on:
  workflow_dispatch:
    inputs:
      mode:
        description: Inspect eligibility or configure the fixed DO NOT PAY staging receiver
        required: true
        default: inspect
        type: choice
        options:
          - inspect
          - configure
      confirm_staging_project_ref:
        description: Type the exact staging project ref
        required: true
        type: string
      confirm_main_commit_sha:
        description: Type the exact full main commit SHA
        required: true
        type: string
      staging_owner_auth_user_id:
        description: Paste only the confirmed staging Owner Auth UUID
        required: true
        type: string
      confirm_synthetic_receiver:
        description: For configure type configure-synthetic-do-not-pay-receiver; for inspect type inspect-only
        required: true
        default: inspect-only
        type: string

permissions:
  contents: read

concurrency:
  group: staging-synthetic-receiver
  cancel-in-progress: false

env:
  STAGING_PROJECT_REF: spzpiyxheappsfyswewl
  PRODUCTION_PROJECT_REF: xzztugbgtulptnbpoelr
  STAGING_POOLER_HOST: aws-1-eu-west-1.pooler.supabase.com

jobs:
  validate-target:
    name: Validate exact synthetic receiver target
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    permissions:
      contents: read
    env:
      REQUESTED_MODE: ${{ inputs.mode }}
      CONFIRMED_STAGING_PROJECT_REF: ${{ inputs.confirm_staging_project_ref }}
      CONFIRMED_MAIN_COMMIT_SHA: ${{ inputs.confirm_main_commit_sha }}
      OWNER_AUTH_USER_ID: ${{ inputs.staging_owner_auth_user_id }}
      SYNTHETIC_RECEIVER_CONFIRMATION: ${{ inputs.confirm_synthetic_receiver }}
    steps:
      - name: Require main, staging, exact source, Owner, and confirmation
        shell: bash
        run: |
          set -euo pipefail

          if [[ "$GITHUB_REF" != 'refs/heads/main' ]]; then
            echo '::error::This workflow may run only from the main branch.'
            exit 1
          fi

          if [[ "$REQUESTED_MODE" != 'inspect' && "$REQUESTED_MODE" != 'configure' ]]; then
            echo '::error::The requested mode is not allowed.'
            exit 1
          fi

          if [[ "$STAGING_PROJECT_REF" == "$PRODUCTION_PROJECT_REF" ||
                "$CONFIRMED_STAGING_PROJECT_REF" != "$STAGING_PROJECT_REF" ||
                "$CONFIRMED_STAGING_PROJECT_REF" == "$PRODUCTION_PROJECT_REF" ]]; then
            echo '::error::The exact non-production staging target was not confirmed.'
            exit 1
          fi

          if [[ ! "$CONFIRMED_MAIN_COMMIT_SHA" =~ ^[0-9a-f]{40}$ ||
                "$CONFIRMED_MAIN_COMMIT_SHA" != "$GITHUB_SHA" ]]; then
            echo '::error::The confirmed commit does not match this exact main workflow commit.'
            exit 1
          fi

          if [[ ! "$OWNER_AUTH_USER_ID" =~ ^[0-9a-f]{8}-[0-9a-f]{4}-[1-8][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$ ]]; then
            echo '::error::The staging Owner Auth user ID is not a canonical UUID.'
            exit 1
          fi

          if [[ "$REQUESTED_MODE" == 'configure' &&
                "$SYNTHETIC_RECEIVER_CONFIRMATION" != 'configure-synthetic-do-not-pay-receiver' ]]; then
            echo '::error::The synthetic DO NOT PAY receiver confirmation phrase is missing.'
            exit 1
          fi

          if [[ "$REQUESTED_MODE" == 'inspect' &&
                "$SYNTHETIC_RECEIVER_CONFIRMATION" != 'inspect-only' ]]; then
            echo '::error::Inspect mode requires the inspect-only confirmation.'
            exit 1
          fi

  synthetic-receiver:
    name: Inspect or configure the synthetic staging receiver
    needs: validate-target
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    environment: staging
    permissions:
      contents: read
    steps:
      - name: Check out exact source
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          persist-credentials: false

      - name: Validate protected staging database inputs
        id: protected-inputs
        shell: bash
        env:
          SUPABASE_DB_PASSWORD: ${{ secrets.SUPABASE_DB_PASSWORD }}
          SUPABASE_CA_CERTIFICATE_PEM: ${{ secrets.SUPABASE_CA_CERTIFICATE_PEM }}
        run: |
          set -euo pipefail

          if ! command -v psql >/dev/null 2>&1; then
            echo '::error::The pinned runner does not provide the PostgreSQL client.'
            exit 1
          fi

          if [[ -z "$SUPABASE_DB_PASSWORD" || -z "$SUPABASE_CA_CERTIFICATE_PEM" ]]; then
            echo '::error::The protected staging environment is missing a database credential or CA certificate.'
            exit 1
          fi

          ca_file="$RUNNER_TEMP/fetanagent-staging-synthetic-receiver-ca.crt"
          umask 077
          printf '%s\n' "$SUPABASE_CA_CERTIFICATE_PEM" > "$ca_file"
          openssl x509 -in "$ca_file" -noout -checkend 0 >/dev/null
          echo "ca_file=$ca_file" >> "$GITHUB_OUTPUT"

      - name: Inspect synthetic receiver eligibility without writes
        shell: bash
        env:
          OWNER_AUTH_USER_ID: ${{ inputs.staging_owner_auth_user_id }}
          PGPASSWORD: ${{ secrets.SUPABASE_DB_PASSWORD }}
          PGHOST: ${{ env.STAGING_POOLER_HOST }}
          PGPORT: '5432'
          PGDATABASE: postgres
          PGUSER: postgres.${{ env.STAGING_PROJECT_REF }}
          PGSSLMODE: verify-full
          PGSSLROOTCERT: ${{ steps.protected-inputs.outputs.ca_file }}
          PGCONNECT_TIMEOUT: '5'
          PGSERVICEFILE: /dev/null
          PGPASSFILE: /dev/null
        run: psql -X --file=infra/sql/staging-synthetic-receiver-inspect.sql

      - name: Configure the fixed synthetic DO NOT PAY receiver
        if: inputs.mode == 'configure'
        shell: bash
        env:
          OWNER_AUTH_USER_ID: ${{ inputs.staging_owner_auth_user_id }}
          PGPASSWORD: ${{ secrets.SUPABASE_DB_PASSWORD }}
          PGHOST: ${{ env.STAGING_POOLER_HOST }}
          PGPORT: '5432'
          PGDATABASE: postgres
          PGUSER: postgres.${{ env.STAGING_PROJECT_REF }}
          PGSSLMODE: verify-full
          PGSSLROOTCERT: ${{ steps.protected-inputs.outputs.ca_file }}
          PGCONNECT_TIMEOUT: '5'
          PGSERVICEFILE: /dev/null
          PGPASSFILE: /dev/null
        run: psql -X --file=infra/sql/staging-synthetic-receiver-configure.sql

      - name: Verify the safe receiver state after configuration
        if: inputs.mode == 'configure'
        shell: bash
        env:
          OWNER_AUTH_USER_ID: ${{ inputs.staging_owner_auth_user_id }}
          PGPASSWORD: ${{ secrets.SUPABASE_DB_PASSWORD }}
          PGHOST: ${{ env.STAGING_POOLER_HOST }}
          PGPORT: '5432'
          PGDATABASE: postgres
          PGUSER: postgres.${{ env.STAGING_PROJECT_REF }}
          PGSSLMODE: verify-full
          PGSSLROOTCERT: ${{ steps.protected-inputs.outputs.ca_file }}
          PGCONNECT_TIMEOUT: '5'
          PGSERVICEFILE: /dev/null
          PGPASSFILE: /dev/null
        run: psql -X --file=infra/sql/staging-synthetic-receiver-inspect.sql

      - name: Remove the temporary CA copy
        if: always()
        shell: bash
        env:
          CA_FILE: ${{ steps.protected-inputs.outputs.ca_file }}
        run: |
          set -euo pipefail
          if [[ -n "${CA_FILE:-}" && -f "$CA_FILE" ]]; then
            rm -f -- "$CA_FILE"
          fi
