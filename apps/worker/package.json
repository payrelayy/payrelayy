{
  "name": "@payreplayy/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "@payreplayy/config": "workspace:*"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "test": "vitest run --passWithNoTests"
  }
}
