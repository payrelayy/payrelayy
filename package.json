{
  "name": "fetanagent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.16.0",
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=11.0.0"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "typecheck": "pnpm -r run build",
    "test": "pnpm build && pnpm -r --filter=!@fetanagent/sql-integration-tests run test",
    "test:infra": "node infra/verify-staging-beta.mjs && node infra/verify-staging-beta-runtime-preflight.mjs && node infra/verify-staging-beta-deploy.mjs && node infra/verify-supabase-staging-bootstrap.mjs && node infra/verify-fetanagent-vm-transition.mjs && node infra/verify-staging-first-owner-bootstrap.mjs && node infra/verify-staging-synthetic-receiver.mjs && node infra/verify-executor-deployment.mjs && node infra/verify-customer-web-image.mjs",
    "test:sql": "node scripts/run-sql-integration.mjs",
    "verify:brand": "node scripts/verify-fetanagent-brand.mjs",
    "lint": "prettier --check --end-of-line auto .",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "prettier": "^3.5.0",
    "tsx": "^4.0.0",
    "typescript": "^5.9.0",
    "vitest": "^3.0.0"
  }
}
